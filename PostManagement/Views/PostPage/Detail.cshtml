@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<ul>
    <li>
        @Model.appUser.FullName
    </li>
    <li>@Model.CreatedDate</li>
    <li>@Model.UpdatedDate</li>
    <li>@Model.Title</li>
    <li>@(Model.PublishStatus == 1 ? "published" : "not published")</li>
    <li>@Model.postCategory.CategoryName - @Model.postCategory.CategoryDescription</li>
</ul>